server:
  port: 8888

spring:
  application:
    name: config
  profiles:
    active: composite
  cloud:
    config:
      discovery:
        enabled: true
      server:
        composite:
          - type: native
            search-locations: classpath:configuration
        bootstrap: true


